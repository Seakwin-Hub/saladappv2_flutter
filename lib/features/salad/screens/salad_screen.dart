import 'package:flutter/material.dart';

class SaladScreen extends StatelessWidget {
  const SaladScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [Text("Salad")]));
  }
}
